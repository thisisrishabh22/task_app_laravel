{
    "private": true,
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "run:dev": "concurrently -n Server,Vite \"php artisan serve\" \"yarn dev\" "
    },
    "devDependencies": {
        "axios": "^1.1.2",
        "concurrently": "^8.0.1",
        "laravel-vite-plugin": "^0.7.2",
        "vite": "^4.0.0"
    }
}
